package ${package}.modules.${moduleName}.${entity};

import lombok.Data;
import lombok.Builder;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
import com.baomidou.mybatisplus.annotations.TableId;
import com.baomidou.mybatisplus.annotations.TableName;
import com.baomidou.mybatisplus.annotations.TableField;
import com.swt.common.validator.group.AddGroup;
import com.swt.common.validator.group.UpdateGroup;

#if(${hasBigDecimal})
import java.math.BigDecimal;
#end
import java.io.Serializable;
import java.util.Date;

#if(${swagger})
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
#end

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@TableName("${tableName}")
#if(${swagger})
@ApiModel(value = "${comments}")
#end
public class ${className}Entity implements Serializable {
	private static final long serialVersionUID = 1L;

#foreach ($column in $columns)
	/**
	 * $column.comments
	 */
#if($column.columnName == $pk.columnName)
	@TableId
#if(${swagger})
	@ApiModelProperty(value = "主键", example = "$column.example", hidden = true)
#end
#else
#if($column.nullAble)
#if($column.attrType == "String")
	@NotBlank(message = "${column.comments}不能为空", groups = {AddGroup.class, UpdateGroup.class})
#else
#if($CrtUserId == ${column.attrName})
	@NotNull(message = "${column.comments}不能为空", groups = {AddGroup.class})
#else
	@NotNull(message = "${column.comments}不能为空", groups = {AddGroup.class, UpdateGroup.class})
#end
#end
#end
#if(${swagger})
	@ApiModelProperty(value = "$column.comments", example = "$column.example", hidden = ${column.hidden}, required = $column.nullAble)
#end
#if($column.startIs)
    @TableField("${column.columnName}")
#end
#end
	private $column.attrType $column.attrname;
#end
}
