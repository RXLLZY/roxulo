package ${package}.${modules}.${moduleName}.${service}.impl;

import org.apache.commons.lang.StringUtils;
import org.springframework.stereotype.Service;
import java.util.Map;
import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import ${package}.common.utils.PageData;
import ${package}.common.utils.PageInfo;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;

import ${package}.${modules}.${moduleName}.${dao}.I${className}Dao;
import ${package}.${modules}.${moduleName}.${entity}.${className};
import ${package}.${modules}.${moduleName}.${service}.I${className}Service;

/**
 * ${comments} 实现
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Service("${classname}Service")
public class ${className}ServiceImpl extends ServiceImpl<I${className}Dao, ${className}> implements I${className}Service {

    @Override
    public PageData<${className}> queryPage(PageInfo pageInfo, ${className} ${classname}) {

        PageData<${className}> resultData = new PageData<>(pageInfo);
        IPage<${className}> page = this.baseMapper.selectPage(
                resultData,
                new QueryWrapper<${className}>()
                        .setEntity(${classname})
        );
        return resultData;
    }

}
